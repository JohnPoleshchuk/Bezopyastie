CREATE DATABASE IF NOT EXISTS `bezopyastie` DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci;

CREATE TABLE 'bracles' (
    'bracle_id' INT NOT NULL PRIMARY KEY,
)

INSERT INTO 'bracles' ('bracle_id')
VALUES
(001),
(002),
(003),
(989);